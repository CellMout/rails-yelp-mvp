<div class="container">
  <h1>Restaurant#index</h1>
  <p>Find me in app/views/restaurant/index.html.erb</p>

  <% @restaurants.each do |restaurant| %>
    <li class="mt-3"><%= link_to restaurant.name, restaurant_path(restaurant), class: "btn btn-primary"%></li>
  <% end %>

  <%= link_to "add new restaurant", new_restaurant_path, class: "btn btn-primary mt-3" %>
</div>
